# Weather Station Server Environment Configuration
# Copy this file to .env and configure with your actual values
# .env is ignored by git (see .gitignore)

# MQTT Configuration
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_TOPIC=rtl_433/#

# InfluxDB Configuration
INFLUXDB_HOST=influxdb
INFLUXDB_PORT=8086
INFLUXDB_DB=weather_data
INFLUXDB_USER=admin
INFLUXDB_PASSWORD=adminpassword

# Python Bridge Configuration
PYTHONUNBUFFERED=1

# Security Note:
# - MQTT_HOST/INFLUXDB_HOST in docker-compose should use service names (mosquitto, influxdb)
# - For external access from client machines, use your server's IP address or hostname
# - Change default passwords in production!
