# InfluxDB Configuration
# IMPORTANT: Copy this file to .env and change the values!
INFLUXDB_ADMIN_USERNAME=admin
INFLUXDB_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
INFLUXDB_ORG=weather
INFLUXDB_BUCKET=weather_data
INFLUXDB_ADMIN_TOKEN=CHANGE_THIS_TOKEN

# Grafana Configuration
GRAFANA_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
# Set this to your public Grafana URL (used for OAuth callbacks, links in emails, etc.)
GRAFANA_ROOT_URL=https://grafana.yourdomain.com

# Cloudflare Tunnel Configuration
# Get your token from: https://one.dash.cloudflare.com/ → Networks → Tunnels
CLOUDFLARE_TUNNEL_TOKEN=CHANGE_THIS_TOKEN

# Timezone Configuration
# IMPORTANT: rtl_433 uses OS time APIs for timestamp formatting
# - On Windows: returns local time by default
# - On Linux: returns UTC unless timezone is configured
# Setting this ensures consistent timestamp handling across platforms
TZ=Europe/Madrid

# MQTT Configuration (optional - for authentication)
# MQTT_USERNAME=mqtt_user
# MQTT_PASSWORD=CHANGE_THIS_PASSWORD

# To generate secure passwords, run:
# openssl rand -base64 32
